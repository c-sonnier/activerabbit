# Name of your application. Used to uniquely configure containers.
service: activerabbit

# Name of the container image.
image: nextsoft-io/activerabbit

# Deploy to these servers.
servers:
  web:
    - 157.245.80.51
  # High-volume ingest worker — dedicated to processing SDK events
  job:
    hosts:
      - 157.245.80.51
    cmd: bundle exec sidekiq -c 25 -q ingest
  # Mixed worker — alerts/default first, then helps with ingest overflow
  job-secondary:
    hosts:
      - 157.245.80.51
    cmd: bundle exec sidekiq -c 15 -q priority,6 -q alerts,5 -q default,3 -q analysis,2 -q mailers -q ingest

# Enable SSL auto certification via Let's Encrypt and allow for multiple apps on a single web server.
# Remove this section when using multiple web servers and ensure you terminate SSL at your load balancer.
#
# Note: If using Cloudflare, set encryption mode in SSL/TLS setting to "Full" to enable CF-to-app encryption.
# Allow up to 3 minutes for db:prepare (migrations) + Rails boot before health check fails.
# Default is 30s which is too short when migrations run on large tables.
deploy_timeout: 180
drain_timeout: 30

proxy:
  ssl: true
  host: app.activerabbit.ai
  healthcheck:
    path: /up
    interval: 1

# Credentials for your image host.
registry:
  # Using GitHub Container Registry
  server: ghcr.io
  username: nextsoft-io

  # Always use an access token rather than real password when possible.
  password:
    - KAMAL_REGISTRY_PASSWORD

# Inject ENV variables into containers (secrets come from .kamal/secrets).
env:
  secret:
    - RAILS_MASTER_KEY
    - DATABASE_URL
    - REDIS_URL
    - SECRET_KEY_BASE
    - APP_REVISION
    - GITHUB_CLIENT_ID
    - GITHUB_CLIENT_SECRET
    - GOOGLE_CLIENT_ID
    - GOOGLE_CLIENT_SECRET
    - STRIPE_SECRET_KEY
    - STRIPE_SIGNING_SECRET
    - STRIPE_PUBLIC_KEY
    - STRIPE_PRICE_TEAM_MONTHLY
    - STRIPE_PRICE_BUSINESS_MONTHLY
    - SMTP_PASSWORD
    - ACTIVERABBIT_DATABASE_PASSWORD
    - PGHOST
    - PGPORT
    - PGUSER
    - PGPASSWORD
    - PGDATABASE
    - OPENAI_API_KEY
    - ANTHROPIC_API_KEY
    - ADMIN_EMAIL_ALLOWLIST
    - RESEND_API_KEY
    - ACTIVERABBIT_SELF_MONITOR
    - SIDEKIQ_REDIS_URL
    - SLACK_CLIENT_ID
    - SLACK_CLIENT_SECRET
    - AR_GH_APP_ID
    - AR_GH_APP_SLUG
    - GITHUB_APP_CLIENT_ID
    - AR_GH_APP_PK_BASE64
    - R2_REPLAY_BUCKET
    - R2_REPLAY_ENDPOINT
    - R2_REPLAY_ACCESS_KEY_ID
    - R2_REPLAY_SECRET_ACCESS_KEY
  clear:
    RAILS_ENV: production
    RAILS_LOG_TO_STDOUT: true
    RAILS_SERVE_STATIC_FILES: true
    APP_HOST: app.activerabbit.ai

    # Performance configuration
    WEB_CONCURRENCY: 2
    RAILS_MAX_THREADS: 5

    # Sidekiq configuration (8 vCPU / 16GB RAM — IO-bound jobs)
    # job: 25 threads (ingest) + job-secondary: 15 threads (alerts/default/analysis/mailers/ingest)
    # + Puma: 2 workers × 5 threads = 10. Total ~50. Pool = 55 for headroom.
    SIDEKIQ_CONCURRENCY: 25
    DB_POOL: 55

    # Security
    FORCE_SSL: true
    DISABLE_RACK_ATTACK: false

# Aliases are triggered with "bin/kamal <alias>". You can overwrite arguments on invocation:
# "bin/kamal logs -r job" will tail logs from the first server in the job section.
aliases:
  console: app exec --interactive --reuse "bin/rails console"
  shell: app exec --interactive --reuse "bash"
  logs: app logs -f
  dbc: app exec --interactive --reuse "bin/rails dbconsole"


# Use a persistent storage volume for sqlite database files and local Active Storage files.
# Recommended to change this to a mounted volume path that is backed up off server.
volumes:
  - "activerabbit_storage:/rails/storage"


# Bridge fingerprinted assets, like JS and CSS, between versions to avoid
# hitting 404 on in-flight requests. Combines all files from new and old
# version inside the asset_path.
asset_path: /rails/public/assets

# Configure the image builder.
builder:
  arch: amd64

  # # Build image via remote server (useful for faster amd64 builds on arm64 computers)
  # remote: ssh://docker@docker-builder-server
  #
  # # Pass arguments and secrets to the Docker build process
  # args:
  #   RUBY_VERSION: ruby-4.0.0
  # secrets:
  #   - GITHUB_TOKEN
  #   - RAILS_MASTER_KEY

# Use a different ssh user than root
# ssh:
#   user: app

# Use accessory services (secrets come from .kamal/secrets).
# Database is external (Ubicloud PostgreSQL), so we only need Redis for Sidekiq
accessories:
  redis:
    image: redis:7.0
    host: 157.245.80.51
    port: 6379
    cmd: redis-server --maxmemory 512mb --maxmemory-policy noeviction --save 60 1000 --appendonly yes --replicaof "no one"
    directories:
      - data:/data
